<form method="post" action="{{ path('clasificacion_delete', {'id': clasificacion.id}) }}" onsubmit="return confirm('Estas seguro?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ clasificacion.id) }}">
    <button class="btn">Borrar</button>
</form>
