{% extends 'base.html.twig' %}

{% block title %}Editar jugador{% endblock %}

{% block body %}
    <div class="container">
        <h1>Editar jugador</h1>
        <div>
            {{ include('jugador/_form.html.twig', {'button_label': 'Guardar'}) }}
        </div>
        {{ include('jugador/_delete_form.html.twig') }}
        <a href="{{ path('equipos_byId', {id: equipo.Id}) }}"><button class="btn">Volver</button></a>
    </div>
{% endblock %}
