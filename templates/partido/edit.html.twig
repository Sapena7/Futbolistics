{% extends 'base.html.twig' %}

{% block title %}Editar Partido{% endblock %}

{% block body %}
    <div class="container">
        <h1>Editar Partido</h1>
        <div>
            {{ include('partido/_form.html.twig', {'button_label': 'Guardar'}) }}
        </div>
            {{ include('partido/_delete_form.html.twig') }}
            <a href="{{ path('partidos_index') }}"><button class="btn">Volver</button></a>
    </div>
{% endblock %}
